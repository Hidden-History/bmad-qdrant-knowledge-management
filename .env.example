# =============================================================================
# Qdrant MCP Knowledge Management - Environment Configuration
# =============================================================================
# Copy this file to .env and customize for your project:
#   cp .env.example .env
#
# All values shown are defaults - only set what you need to change.
# =============================================================================

# -----------------------------------------------------------------------------
# QDRANT CONNECTION
# -----------------------------------------------------------------------------

# Qdrant server URL
# - Local Docker: http://localhost:6333
# - Qdrant Cloud: https://your-cluster-url.qdrant.io
QDRANT_URL=http://localhost:6333

# Qdrant API key (required for Qdrant Cloud, optional for local)
# Leave empty for local development without authentication
QDRANT_API_KEY=

# -----------------------------------------------------------------------------
# COLLECTION NAMES
# -----------------------------------------------------------------------------
# Customize these for your project. The defaults work for most BMAD projects.

# Main knowledge collection - stores all knowledge types except best_practice
QDRANT_KNOWLEDGE_COLLECTION=bmad-knowledge

# Best practices collection - stores universal patterns discovered by agents
QDRANT_BEST_PRACTICES_COLLECTION=bmad-best-practices

# -----------------------------------------------------------------------------
# EMBEDDING CONFIGURATION
# -----------------------------------------------------------------------------

# Embedding model for vector generation
# Default uses sentence-transformers which works well for most use cases
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Vector dimension (must match your embedding model)
# - all-MiniLM-L6-v2: 384
# - all-mpnet-base-v2: 768
# - text-embedding-ada-002: 1536
EMBEDDING_DIMENSION=384

# -----------------------------------------------------------------------------
# PROJECT METADATA
# -----------------------------------------------------------------------------

# Your project name - used in metadata and logging
PROJECT_NAME=my-project

# -----------------------------------------------------------------------------
# VALIDATION SETTINGS (Advanced)
# -----------------------------------------------------------------------------

# Minimum content length for knowledge entries (characters)
MIN_CONTENT_LENGTH=100

# Maximum content length for knowledge entries (characters)
MAX_CONTENT_LENGTH=50000

# Semantic similarity threshold for duplicate detection (0.0 - 1.0)
# Higher values = stricter matching (fewer false positives)
# Lower values = more matches (fewer false negatives)
SIMILARITY_THRESHOLD=0.85
